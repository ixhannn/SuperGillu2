# Loaded only by `npm run dev:e2e` (vite --mode e2e). Enables the in-app E2E
# harness so the auth gate is bypassed and a seeded couple profile is rendered,
# for local preview/screenshots and Playwright. Not a secret.
VITE_E2E=1
